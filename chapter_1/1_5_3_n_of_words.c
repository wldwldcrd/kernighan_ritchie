#include <stdio.h>

int main() {
    int nos = 0;
    int c = 0;
    int is_word = 0; 

    // // replacing special symbols
    // while ((c = getchar()) != '\n'){
    //     if (c == '\t') {
    //         printf("\\t");
    //     } else if (c == '\\') {
    //         printf("\\\\");
    //     } else {
    //         putchar(c);
    //     }
    // }

    // // calculating number of words (sep ' ' or '\t')
    // while ((c = getchar()) != '\n') {
    //     if (is_word && ((c == ' ') || (c == '\t'))) {
    //         is_word = 0;
    //         nos += 1;
    //     }
    //     is_word = !((c == ' ') || (c == '\t'));
    // }
    // nos += is_word;
    // printf("\nNumber of words = %d\n", nos);
    
}